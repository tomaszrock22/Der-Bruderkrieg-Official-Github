GER_PARTISANS_CATEGORY = {
	GER_DEFECTORS = {
          allowed = {
		  tag = BEI
		  }
		  available = {
		  }
		  visible = {
		  tag = BEI
		  }
		  cost = 50
		  fire_only_once = yes
		  days_remove = 15
          is_good = yes
          complete_effect = {
					SHX = {
						set_country_flag = Join_Beyiang
					}
		  }
     }
	 GER_PARTISAN_PEOPLE = {
          allowed = {
		  tag = BEI
		  }
		  available = {
		  }
		  visible = {
		  tag = BEI
		  }
		  cost = 50
		  fire_only_once = yes
		  days_remove = 15
          is_good = yes
      complete_effect = {

      }
     }
    }


GER_prepare_for_the_kaiser_return = {
	GER_hold_pro_monarchist_rallies = {

		icon = generic_construction

		available = {
			tag = GER

		}

		fire_only_once = yes

		cost = 30
		days_remove = 15

		modifier = {

		}

		ai_will_do = {
			factor = 20
		}

		visible = {
		tag = GER
		}

		remove_effect = {
			add_popularity = {
				ideology = monarchy
				popularity = 0.03
			}
		}
	}
}
