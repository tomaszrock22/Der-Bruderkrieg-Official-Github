GER_PARTISANS_CATEGORY = {
	GER_DEFECTORS = {
          allowed = {
		  tag = BEI
		  }
		  available = {
		  }
		  visible = {
		  tag = BEI
		  }
		  cost = 50
		  fire_only_once = yes
		  days_remove = 15
          is_good = yes
          complete_effect = {
					SHX = {
						set_country_flag = Join_Beyiang
					}
		  }
     }
	 GER_PARTISAN_PEOPLE = {
          allowed = {
		  tag = BEI
		  }
		  available = {
		  }
		  visible = {
		  tag = BEI
		  }
		  cost = 50
		  fire_only_once = yes
		  days_remove = 15
          is_good = yes
      complete_effect = {
        
      }
     }
    }
