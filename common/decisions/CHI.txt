CHI_ENVOY_CATEGORY = {
	CHI_ENVOY_SHX = {
          allowed = {
		  tag = CHI
		  }
		  available = {
		  }
		  visible = {
		  tag = CHI
		  }
		  cost = 50
		  fire_only_once = yes
		  days_remove = 15
          is_good = yes
          complete_effect = {
					set_country_flag = Join_China
		  }
     }
	 CHI_ENVOY_XSM = {
          allowed = {
		  tag = CHI
		  }
		  available = {
		  }
		  visible = {
		  tag = CHI
		  }
		  cost = 50
		  fire_only_once = yes
		  days_remove = 15
          is_good = yes
          complete_effect = {
					set_country_flag = Join_China
		  }
     }
	 CHI_ENVOY_SIC = {
          allowed = {
		  tag = CHI
		  }
		  available = {
		  }
		  visible = {
		  tag = CHI
		  }
		  cost = 50
		  fire_only_once = yes
		  days_remove = 15
          is_good = yes
          complete_effect = {
					set_country_flag = Join_China
		  }
     }
	 CHI_ENVOY_YUN = {
          allowed = {
		  tag = CHI
		  }
		  available = {
		  }
		  visible = {
		  tag = CHI
		  }
		  cost = 50
		  fire_only_once = yes
		  days_remove = 15
          is_good = yes
          complete_effect = {
					set_country_flag = Join_China
		  }
     }
	 CHI_ENVOY_GXC = {
          allowed = {
		  tag = CHI
		  }
		  available = {
		  }
		  visible = {
		  tag = CHI
		  }
		  cost = 50
		  fire_only_once = yes
		  days_remove = 15
          is_good = yes
          complete_effect = {
					set_country_flag = Join_China
		  }
     }
}
