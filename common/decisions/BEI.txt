BEI_ENVOY_CATEGORY = {
	BEI_ENVOY_SHX = {
          allowed = {
		  tag = BEI
		  }
		  available = {
		  }
		  visible = {
		  tag = BEI
		  }
		  cost = 50
		  fire_only_once = yes
		  days_remove = 15
          is_good = yes
          complete_effect = {
					SHX = {
						set_country_flag = Join_Beyiang
					}
		  }
     }
	 BEI_ENVOY_XSM = {
          allowed = {
		  tag = BEI
		  }
		  available = {
		  }
		  visible = {
		  tag = BEI
		  }
		  cost = 50
		  fire_only_once = yes
		  days_remove = 15
          is_good = yes
          complete_effect = {
					set_country_flag = Join_Beyiang
		  }
     }
	 BEI_ENVOY_SIC = {
          allowed = {
		  tag = BEI
		  }
		  available = {
		  }
		  visible = {
		  tag = BEI
		  }
		  cost = 50
		  fire_only_once = yes
		  days_remove = 15
          is_good = yes
          complete_effect = {
					set_country_flag = Join_Beyiang
		  }
     }
	 BEI_ENVOY_YUN = {
          allowed = {
		  tag = BEI
		  }
		  available = {
		  }
		  visible = {
		  tag = BEI
		  }
		  cost = 50
		  fire_only_once = yes
		  days_remove = 15
          is_good = yes
          complete_effect = {
		  }
     }
	 BEI_ENVOY_GXC = {
          allowed = {
		  tag = BEI
		  }
		  available = {
		  }
		  visible = {
		  tag = BEI
		  }
		  cost = 50
		  fire_only_once = yes
		  days_remove = 15
          is_good = yes
          complete_effect = {
					set_country_flag = Join_Beyiang
		  }
     }
}
